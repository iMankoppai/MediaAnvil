# MediaAnvil 使用说明

**[English →](USER_GUIDE.en.md)**

适用版本：MediaAnvil Qt 1.0.1（Windows）

MediaAnvil 是一款本地运行的多媒体工具箱，可用于播放音频与同步歌词、编辑音频标签和封面、转换歌词/字幕、转换音频和图片格式，以及根据音频标签批量重命名文件。媒体处理在本机完成。

## 1. 安装与启动

1. 解压或复制完整的 `MediaAnvilQt` 文件夹。
2. 双击文件夹中的 `MediaAnvilQt.exe`。
3. 程序启动后默认打开“音频预览”页面。

程序每次启动都会以 `1440 × 900`（16:10）的窗口外框作为默认目标，并自动显示在主屏幕中央。高 DPI 或可用空间较小的屏幕会保持比例缩小，避免窗口超出屏幕。

请保留整个 `MediaAnvilQt` 文件夹，不要只复制 EXE。程序运行所需的组件位于同一文件夹的 `_internal` 目录中。

Windows 首次运行时可能显示 SmartScreen 提示。请先确认文件来源可信，再选择“更多信息”并继续运行。

## 2. 界面与通用操作

左侧导航包含以下页面：

- 音频预览
- 音频标签编辑
- 歌词 / 字幕转换
- 音频格式转换
- 图片格式转换
- 批量重命名
- 设置
- 关于

多数页面支持把文件或文件夹直接从 Windows 资源管理器拖入窗口。拖入文件夹时，是否扫描子文件夹取决于“设置 → 文件扫描 → 包含子文件夹”。批量重命名页也有自己的“包含子文件夹”选项。

转换页中的文件默认全部勾选。取消某个文件的勾选后，该文件不会参与本次处理。“移除”只会把文件从当前列表移除，不会删除磁盘上的原文件。

## 3. 音频预览

### 打开并播放音频

1. 打开“音频预览”。
2. 点击“选择音频文件…”，选择要播放的音频。
3. 点击中间的播放按钮开始播放，再次点击可暂停。
4. 左右按钮分别后退 30 秒和前进 30 秒。

可以点击或拖动进度条跳转播放位置；下方音量条用于调节音量。按空格键也可以播放或暂停。

### 同步歌词

如果音频旁边有同名 `.lrc`、`.srt` 或 `.vtt` 文件，例如：

```text
歌曲.mp3
歌曲.lrc（也可以是歌曲.srt 或歌曲.vtt）
```

也支持保留音频扩展名的命名方式，例如 `歌曲.wav.vtt`、`歌曲.flac.srt` 或 `歌曲.mp3.lrc`。同一种字幕格式的两种命名同时存在时，优先使用包含完整音频文件名的版本。

程序会在开启“自动加载同名歌词”时直接读取文件，不会转换或修改原文件。默认识别顺序为 LRC、SRT、VTT、MP3 内嵌同步歌词。

开启“优先使用 MP3 内嵌歌词”后，MP3 会优先读取可用的内嵌同步歌词；内嵌歌词不可用时，再按照 LRC、SRT、VTT 的顺序查找外置文件。此开关不影响其他音频格式。

播放时当前歌词会高亮并自动居中。点击任意歌词或字幕可跳转到对应时间。SRT/VTT 的多行内容会保留换行，字幕结束后会取消高亮。没有同步歌词时不影响音频播放。

支持预览：MP3、WAV、FLAC、M4A、AAC、OGG。

## 4. 音频标签编辑

### 编辑基本信息

1. 打开“音频标签编辑”。
2. 点击“选择音频文件…”。
3. 修改歌名、歌手和专辑。
4. 选择保存方式和输出目录。
5. 点击“保存到音频”。

默认保存方式为“另存为”，原文件不会被修改。只有明确选择“覆盖原文件”时，保存才会替换源文件。

可写入标签的格式：MP3、FLAC、M4A、OGG、Opus。WAV 当前只支持读取信息，不支持保存标签、歌词或封面。AAC 不属于当前标签编辑支持格式。

### 歌词操作

- “导入歌词 / 字幕…”：可直接选择 LRC、SRT 或 VTT。SRT/VTT 会在程序内部转换为 LRC 后等待写入，不会生成额外文件或修改原字幕。
- “导出歌词…”：导出当前音频中的内嵌歌词。
- “移除歌词（保存时生效）”：勾选后，下次保存时移除内嵌歌词。

写入音频标签时统一使用 LRC 时间歌词，以保持内嵌歌词兼容性。导出内嵌同步歌词时仍输出 LRC。

### 封面操作

- “选择图片…”：选择新封面后会立即打开矩形裁剪窗口；拖拽创建或移动裁剪框，也可输入横向起点、纵向起点、宽度和高度。默认选择完整图片，确认后导入裁剪结果，取消则保持当前封面不变。
- “导出图片…”：导出当前内嵌封面。
- “移除封面（保存时生效）”：勾选后，下次保存时移除内嵌封面。

支持选择 JPG、JPEG、PNG、WebP 和 BMP 图片。封面预览会随可用空间自动放大；裁剪结果可以是正方形、横向长方形或纵向长方形。程序会在写入前转换为音频标签支持的图片数据。

### 智能匹配关联文件

“智能匹配关联文件”默认收起。展开后可扫描音乐文件夹，并按文件名寻找歌词和封面候选。

智能匹配也支持保留音频扩展名的关联文件，例如 `歌曲.wav.vtt`、`歌曲.wav.png`、`歌曲.flac.lrc`。同格式下，这种包含完整音频文件名的候选会优先于 `歌曲.vtt` 或 `歌曲.png`。

“清空文件”只会清除当前扫描结果和候选列表，不会删除磁盘上的音频、歌词或图片文件。

匹配优先顺序为：完全同名、忽略空格、去除“副本”或括号编号。存在多个同等级候选时，程序不会擅自选择，需要手动确认。

## 5. 歌词 / 字幕转换

支持 LRC、SRT、VTT 三种格式相互转换。

1. 点击“添加文件”，或把文件拖入虚线框。
2. 确认要转换的文件已勾选。
3. 选择输出格式。
4. 如有需要，设置 LRC 最后一句的持续时间。
5. 选择输出目录；留空时保存到源文件所在文件夹。
6. 点击“开始批量转换”。
7. 在右侧选择结果并查看文本内容。

LRC 通常没有结束时间。转换为 SRT 或 VTT 时，普通歌词行会在下一句开始时结束，最后一句使用设置中的持续时间，默认 5 秒。

输出文本使用带 BOM 的 UTF-8 编码。已有同名文件时会自动使用 `_1`、`_2` 等新文件名，不会覆盖已有文件。

## 6. 音频格式转换

支持 MP3、WAV、FLAC、M4A、AAC、OGG 的输入与输出。

1. 点击“添加文件”，或把音频拖入虚线框。
2. 选择输出格式。
3. 根据格式选择码率、压缩等级或质量等级。
4. 按需选择采样率和声道；“保持原始”通常适合大多数情况。
5. 决定是否保留音频标签。
6. 选择输出目录并点击“开始批量转换”。

常用参数：

| 项目 | 可选值 |
| --- | --- |
| MP3 码率 | 128、192、256、320 kbps |
| AAC / M4A 码率 | 96、128、192、256 kbps |
| FLAC 压缩等级 | 0、5、8 |
| OGG 质量等级 | 3、5、7、9 |
| 采样率 | 保持原始、44100、48000、96000 Hz |
| 声道 | 保持原始、单声道、立体声 |

输出格式与源文件格式相同时，程序会拒绝无意义的转换。音频转换由随软件提供的 FFmpeg 完成，无需另外安装。

## 7. 图片格式转换

支持 JPG/JPEG、PNG、WebP、BMP 相互转换。

1. 点击“添加图片”，或把图片拖入虚线框。
2. 选择输出格式。
3. 输出 JPG 或 WebP 时设置 1–100 的图片质量。
4. 选择输出目录并点击“开始批量转换”。
5. 在右侧查看转换结果和图片预览。

程序保持原始图片宽高。PNG 和 WebP 可以保留透明区域；透明图片转换为 JPG 或 BMP 时使用白色背景。

## 8. 批量重命名

批量重命名根据音频标签生成新文件名，不会重新编码音频，也不会修改标签内容。

支持：MP3、FLAC、M4A、OGG、Opus。

1. 点击“添加文件”，或把音频/文件夹拖入页面。
2. 选择重命名模板。
3. 点击变量按钮可向模板插入字段。
4. 点击刷新按钮生成预览。
5. 检查原文件名、新文件名和状态。
6. 勾选需要执行的项目，点击“开始重命名”。

可用变量：

| 变量 | 含义 |
| --- | --- |
| `{artist}` | 歌手 |
| `{title}` | 歌名 |
| `{album}` | 专辑 |
| `{track}` | 曲目号 |
| `{year}` | 年份 |

“缺少字段时使用原文件名代替”可以避免因标签不完整而直接失败。非法文件名字符会被安全处理；重名冲突会在预览中显示。

“导出预览”可把预览结果保存为 CSV。“撤销上次重命名”只撤销最近一次成功批次；若原文件名已被其他文件占用，程序不会覆盖该文件。

## 9. 设置

设置页中的选项会用于后续任务：

- 常规：默认输出位置、默认保存方式、界面语言。
- 音频转换：默认 MP3/AAC 码率、保持采样率、保持声道、保留标签。
- 图片转换：默认 JPG/WebP 质量和保持原始尺寸。
- 歌词与预览：LRC 最后一句持续时间、默认音量、自动加载同名歌词。
- 文件扫描：是否包含子文件夹。

修改后点击“保存设置”。“恢复默认设置”会把页面中的选项恢复为默认值，仍需点击“保存设置”才会写入配置。

### 切换中文和英文

打开“设置 → 常规 → 语言”，选择“简体中文”或“English”，然后点击“保存设置”。界面会立即切换，并在下次启动时保持所选语言。

设置文件位于：

```text
%APPDATA%\MediaAnvilQt\settings.json
```

如果设置文件损坏，程序会自动恢复安全默认值。

## 10. 输出与文件安全

- 转换功能不会覆盖源文件。
- 已有同名输出时自动生成安全的新文件名。
- 标签编辑默认“另存为”。
- 覆盖标签前会先处理临时副本并校验。
- “移除”和“清空”只影响软件中的任务列表，不会删除磁盘文件。
- 批量重命名只修改文件名，不修改音频内容。

处理期间内容区可能会暂时禁用。请等待任务完成后再关闭程序，避免中断写入。

## 11. 常见问题

### 双击 EXE 没有反应

确认 `_internal` 文件夹与 `MediaAnvilQt.exe` 位于同一发布目录。不要只复制 EXE。

### 文件拖入后没有出现

确认文件扩展名是当前页面支持的格式。拖入文件夹时，检查“包含子文件夹”设置是否符合需要。

### 音频可以播放，但没有歌词

确认 LRC 与音频位于同一目录、主文件名完全相同，并检查“自动加载同名歌词”是否开启。

### 找不到转换后的文件

输出目录留空时，结果保存在源文件所在文件夹。也可以在结果区点击“打开所在位置”。

### 无法保存 WAV 标签

WAV 当前只支持信息读取。若需要可写入标签的文件，可先转换为 MP3、FLAC、M4A、OGG 或 Opus。

### 目标格式和源格式相同

音频转换不执行同格式重新编码。请选择不同的输出格式。

### JPG 转换后没有透明背景

JPG 不支持透明背景。程序会用白色填充透明区域；需要透明背景时请选择 PNG 或 WebP。

## 12. 第三方组件

软件随附 Qt/PySide6、ICU、FFmpeg 和 FFplay。许可信息位于发布文件夹中的 `THIRD_PARTY_NOTICES.md` 和 `licenses` 目录。

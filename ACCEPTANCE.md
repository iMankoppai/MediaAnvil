# MediaAnvil 多环境人工验收清单

> 适用版本：1.4.0（Windows）
> 用途：在**我无法覆盖的环境**里人工验证。自动化测试只能证明"在这台开发机上正确"，
> 换机器、换用户名、换缩放比例的结果必须由人在真机上确认。

---

## 为什么要做这份清单

我的测试全部跑在同一台机器、同一个账户上。以下情况**自动化测试无法覆盖**：

| 环境差异 | 为什么可能出问题 |
|---|---|
| 没有 Python 的电脑 | 打包版必须自带全部依赖；万一漏了，只有干净机器能暴露 |
| 中文用户名 / 中文路径 | Windows 上中文路径是经典翻车点（编码、临时文件、FFmpeg 调用） |
| 超长路径 | Windows 路径长度限制 |
| 显示缩放 125% / 150% / 200% | 界面可能错位、按钮被裁掉、文字重叠 |
| Windows 10 vs 11 | 系统控件行为有差异 |
| 用户目录被重定向 | 设置文件和最近目录可能存不下 |

**请按下面顺序做**。每项都给了"怎么点"和"该看到什么"。发现问题就把编号告诉我。

---

## 第一部分：准备

### 1.1 下载

从 Releases 页面下载 `MediaAnvil-v1.4.0-Windows-x64.zip`：

```
https://github.com/iMankoppai/MediaAnvil/releases/tag/v1.4.0
```

**解压到完整文件夹**，不要只把 EXE 拖出来。运行时必须保留 `_internal` 目录。

### 1.2 校验下载完整（可选但推荐）

压缩包里带了一个 `.sha256` 文件。在解压目录打开 PowerShell：

```powershell
Get-FileHash .\MediaAnvil-v1.4.0-Windows-x64.zip -Algorithm SHA256
```

把输出的哈希和 `.sha256` 文件里的字符串对比，**应完全一致**。

### 1.3 记录环境

每换一个环境，先记下这几项：

| 项目 | 你的环境 |
|---|---|
| Windows 版本 | （10 / 11，具体版本号） |
| 显示缩放 | （100% / 125% / 150% / 200%） |
| 用户名是否含中文 | （是 / 否） |
| 是否装有 Python | （是 / 否） |
| 测试路径是否含中文 | （是 / 否） |

---

## 第二部分：验收项目

### A. 干净环境启动（最重要）

**目的**：确认打包版真的不依赖 Python。

| 编号 | 步骤 | 期望结果 |
|---|---|---|
| A1 | 在一台**没装 Python** 的电脑（或一个没装过 Python 的干净 Windows 账户）上解压并双击 `MediaAnvilQt.exe` | 程序正常打开，停在"音频预览"页 |
| A2 | 观察是否弹出缺 DLL、缺 Python、缺模块之类的错误 | **不应有任何报错** |
| A3 | 打开"关于"页 | 显示版本 `v1.4.0` |
| A4 | 关掉程序，再开一次 | 正常打开（确认不是首次偶然成功） |

**风险说明**：如果这步失败，说明打包漏了依赖。这是最严重的问题。

---

### B. 中文用户名与中文路径

**目的**：确认中文路径全程可用。

| 编号 | 步骤 | 期望结果 |
|---|---|---|
| B1 | 在一个**用户名含中文**的账户下运行（例如 `C:\Users\张三\`） | 程序正常打开 |
| B2 | 准备一个中文路径的文件夹，例如 `D:\我的音乐\测试\` | — |
| B3 | 把几个音频放进该文件夹，用"音频预览"打开其中一个 | 能播放，歌词能显示 |
| B4 | 用"音频标签编辑"修改歌名，保存 | 保存成功，中文标签正确显示 |
| B5 | 用"音频合并 / 分割"把两个文件合并 | 生成新文件，文件名含中文且正确 |
| B6 | 用"歌词 / 字幕转换"转换一个中文名 `.lrc` | 转换成功 |

**风险说明**：中文路径可能让 FFmpeg 调用失败或文件名乱码。

---

### C. 超长路径

| 编号 | 步骤 | 期望结果 |
|---|---|---|
| C1 | 建一个**很深的目录**（例如 10 层以上，路径接近 260 字符），放一个音频进去 | — |
| C2 | 尝试打开、转换、保存该文件 | 能正常处理，或给出**清楚的错误提示**（不能闪退） |

**说明**：Windows 默认路径上限约 260 字符。如果程序处理不了，应该给出明确提示而不是崩溃。

---

### D. 显示缩放（容易出问题）

**目的**：确认界面在高分屏上不错位。

对 **100% / 125% / 150% / 200%** 各做一遍：

设置方法：桌面右键 → 显示设置 → 缩放与布局 → 更改缩放比例。
（部分比例需要**注销后重新登录**才生效。）

| 编号 | 步骤 | 期望结果 |
|---|---|---|
| D1 | 在每个缩放下打开程序 | 窗口大小合适，不超出屏幕 |
| D2 | 逐个点开左侧**全部 9 个页面** | 没有按钮被裁掉、没有文字重叠、没有横向滚动条 |
| D3 | 看每个页面底部的按钮栏（如"开始转换""保存到音频"） | 完整可见，没被遮住 |
| D4 | 把窗口**拖到最小**（约 760×480） | 内容自动改为上下排列，仍能操作 |
| D5 | 把窗口**最大化** | 布局正常，没有大片空白或拉伸变形 |

**风险说明**：这是最容易出问题的一项。我的测试只能验证相对关系，验证不了真实缩放下的像素效果。

---

### E. Windows 10 与 Windows 11

| 编号 | 步骤 | 期望结果 |
|---|---|---|
| E1 | 在 Windows 10 上完整走一遍 B、D 两组 | 与 Windows 11 表现一致 |
| E2 | 观察窗口标题栏、滚动条、下拉框外观 | 正常，没有显示异常 |

---

### F. 用户目录被重定向或不存在

| 编号 | 步骤 | 期望结果 |
|---|---|---|
| F1 | 在用户"音乐""图片"目录**被重定向**到其他盘的环境下运行 | 程序正常，文件对话框能打开 |
| F2 | 打开一个文件选择对话框（如"选择音频文件…"） | 能正常打开，不报错 |
| F3 | 改一下设置（如语言），关闭再打开 | 设置被正确保存和恢复 |

---

### G. 文件状态覆盖

| 编号 | 步骤 | 期望结果 |
|---|---|---|
| G1 | 什么都不选，直接点"开始转换" | 给出提示"请先添加文件"，不崩溃 |
| G2 | 只加 **1 个**文件，选"合并" | 提示"合并至少需要两个音频文件" |
| G3 | 加 **10 个以上**文件，批量转换 | 全部处理完成，进度正常 |
| G4 | 处理过程中点"取消任务" | 任务停止，未完成的临时文件被清理 |

---

### H. 无默认 Markdown 程序

| 编号 | 步骤 | 期望结果 |
|---|---|---|
| H1 | 在一台**没有装任何 Markdown 阅读器**的电脑上，打开"关于"→"使用说明" | 在**程序内部**正常显示，不调用外部程序 |
| H2 | 点击说明里的一个网页链接 | **只复制链接**，不打开浏览器 |
| H3 | 点击说明里指向另一个文档的链接（如 `USER_GUIDE.en.md`） | 在程序内跳转到该文档 |

---

### I. 界面语言

| 编号 | 步骤 | 期望结果 |
|---|---|---|
| I1 | 设置 → 语言 → 切换为 English | 界面即时变英文，**不需要重启** |
| I2 | 在英文界面下走一遍主要功能 | 文案正确，没有残留中文（专有名词除外） |
| I3 | 切回中文 | 恢复正常 |

---

## 第三部分：记录表

每完成一个环境，填一行：

| 环境 | 系统 | 缩放 | 中文用户名 | 无 Python | 结果 | 备注 |
|---|---|---|---|---|---|---|
| 1 | | | | | | |
| 2 | | | | | | |
| 3 | | | | | | |

**发现问题时请记录**：

| 编号 | 环境 | 现象 | 能否重现 |
|---|---|---|---|
| | | | |

---

## 第四部分：我最担心的地方

按风险从高到低，如果时间有限，优先测这些：

1. **D（显示缩放）** —— 最容易出问题，我的测试完全覆盖不到真实像素效果
2. **A（无 Python 环境）** —— 一旦失败就是"用户根本打不开"，最严重
3. **B（中文路径）** —— Windows 上的经典翻车点，且涉及 FFmpeg 调用
4. **C（超长路径）** —— 可能直接报错

**已经由自动化测试覆盖、你可以不用重复验证的**：

- 固定底栏不会遮住滚动内容（有 2 个测试）
- 最近目录记忆与失效回退
- 文档缺失/编码错误时的提示
- 冻结版能否读出内置文档
- 中文界面文案完整性

---

## 附：发现问题时怎么告诉我

请提供：

1. **编号**（如 D3）
2. **环境**（系统版本 + 缩放比例）
3. **现象**（看到什么，最好截图）
4. **能否重现**（再试一次还会吗）

有截图最好——界面错位这类问题，一张图比一段描述清楚得多。
